# Install as /etc/systemd/system/caddy.service.d/jarvis.conf.
# Keep the referenced file root-owned (0644 is sufficient: it contains only a
# public hostname, never a token or database credential).
[Service]
EnvironmentFile=/etc/jarvis/public.env
