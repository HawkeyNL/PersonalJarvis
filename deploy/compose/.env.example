# Copy to deploy/compose/.env only for the local SurrealDB development stack.
# Never reuse this development credential in production.
SURREAL_ROOT_USER=root
SURREAL_ROOT_PASS=change-this-dev-root-password
SURREAL_PORT=8000
